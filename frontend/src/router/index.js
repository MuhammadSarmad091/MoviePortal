import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  // Routes will be added here in future steps
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
